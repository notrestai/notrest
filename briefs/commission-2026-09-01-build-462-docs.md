# Commission — 4.6.2 build lane D: DOCS CURRENCY (no refuter; the seat gates by doctor/eval + lint)
COMMISSION (Director session 3, banked 2026-09-01T23:45Z before dispatch; owner GO 2026-09-01: 'fix the remaining items as well and then ship the 4.6.2'). Repo: /Users/ethanabot/Desktop/oracle-suite-plugin, HEAD 54cb763 = v4.6.1. The findings you are fixing are in docs/DOCKET-4.6.2.md (read it first; every item carries a repro).
LAWS: (1) TOUCH-ONLY below is absolute — any file outside it is the seat's or another lane's; if a fix needs a file outside scope, STOP and return the exact need. (2) Red-first: every behavior fix ships with a fixture arm that was watched RED on the old code and GREEN on the new (paste both). (3) Never run the CLI's consumer plugin flow (install/update/marketplace subcommands) on this machine. (4) Never write COORD.md, COORD-AGENTS.md, spend/ledger.md, briefs/, CHANGELOG.md, any manifest version, docs/oracle-skill-flow.html, evals/golden-release-surface.txt — the seat stamps the release. (5) Never spawn sub-agents. (6) Never git commit/push/stage; leave changes in the working tree. (7) Python 3.9 stdlib only; macOS stock tools only (no timeout binary, no gdate, no flock binary).
RETURN (<= 60 lines): per item — what changed (file:line), the red-then-green fixture evidence, exit codes of the fixture(s) you touched run whole; then 'git diff --stat' of your scope; then anything left undone and why. No prose beyond that. You are a PERSISTENT lane: the seat will resume you for feedback rounds, so keep your own working notes.
TOUCH-ONLY: README.md ; plugins/notrest/README.md ; docs/TUTORIAL.md ; plugins/notrest/skills/fable-mode/SKILL.md ; docs/UNDERSTANDING.md ; docs/MAP.md (content only — the seat sets its version/commit line at ship) ; .claude-plugin/marketplace.json (the notrest entry's DESCRIPTION string only — never the version fields) ; START-HERE.md ; HANDOFF.md ; STATE.md.
ITEMS: M1 — README.md skill table gains rows for beam, mentor, tieredswarm in the table's own style (read each SKILL.md description for the row text); the 'Thirty-two' claim then matches 32 rows. M2 — mentor named with one honest line in plugins/notrest/README.md, docs/TUTORIAL.md, and the marketplace description's skill enumeration (insert it where the list flows; keep 'thirty-two'). F1 — README.md:71,76 and fable-mode/SKILL.md:101-105,265: restate the offload law as it is enforced by plugins/notrest/hooks/spawn-gate.sh and printed by hooks/session-start.sh (opus default; explicit sonnet lawful only for a brief-declared mechanical/DRAFT-tier job; never haiku; never fork; omitting model is a violation) with the amendment date 2026-08-30; quote the gate, do not invent wording. F3 — docs/UNDERSTANDING.md:183 present-tense state → v4.6.2 (32 skills) or a version-neutral phrasing; docs/MAP.md: add the things 4.6.2 introduces once the other lanes' file names are known (hooks-stdin-fixture.sh, the doctor ROSTER PARITY check, removal of plugins/notrest/graph/) — check the tree at the end of your run for what actually landed and map only that. F2 — REWRITE START-HERE.md, HANDOFF.md, STATE.md for the 4.6.2 state FROM THE LEDGER: read COORD.md's last ~40 lines and CHANGELOG 4.5.0-4.6.1 + docs/DOCKET-4.6.2.md; state version v4.6.2 (shipping), 32 skills, doctor exit 5 with its two known warns and eval exit 0 as the expected gates, the auto-continuation packet as the resume mechanism, the NAS-side plan of record, no live line; every command a reader is told to run must run as written (run each read-only one yourself); then 'python3 plugins/notrest/skills/sessionend/scripts/starthere_lint.py check --file START-HERE.md --root .' must exit 0. Do not cite a commit hash for 4.6.2 (it does not exist yet) — say 'git log -1'.
DONE-WHEN: starthere_lint exit 0; 'grep -c 2026-08-30' >= 1 in README.md and fable-mode/SKILL.md; README table row count == 32; mentor present in all four surfaces; return shape satisfied.
