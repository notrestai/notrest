# 4.8.0 lane A — the atlas skill (bank, wire, key). model: opus — tier: judgment
COMMISSION (Director session 3, banked 2026-09-06T08:13Z before dispatch; owner GO 2026-09-06 'repo is private now, go for 4.8'). Repo /Users/ethanabot/Desktop/oracle-suite-plugin, HEAD afe2270 = v4.7.1, origin PRIVATE. Docket: docs/DOCKET-4.8.md (read first). LAWS: TOUCH-ONLY absolute; red-first arms; no commits/staging; never CHANGELOG/manifest versions/HTML/golden list (seat); no consumer plugin flow; no sub-agents; never claude -p or a real auto-run; py3.9 stdlib, stock macOS; hooks keep fast paths + fail-open; persistent lane — feedback resumes you; put every CLI/file contract in your FIRST return line. RETURN <= 60 lines.
TOUCH-ONLY: plugins/notrest/skills/atlas/** (new: SKILL.md, scripts/atlas.py, scripts/fixture.sh, references/), plugins/notrest/.access/keys.sha256 (new keyring; seed with ONE key the seat mints after you land — leave the file with a header comment only), plugins/notrest/hooks/atlas-bank-hook.sh (new: the tracked git post-commit hook body that calls atlas.py bank; lane H wires nothing here).
ITEMS: docket A key minting/verification (`atlas.py key --mint --label X` prints the key once, appends sha256:label:date; `key --check` exit 0/7; `key --revoke <label>`), docket B bank/wire/status/adapters exactly as written (status derivation law, snapshot + board, file adapter real, http adapter stubbed behind one function marked unverified, born-red proof, idempotent wire). SKILL.md per the skill contract (trigger, contract, self-check, finish, exit codes) — eval WORKER-CONTRACT/TRIGGER-SANITY/SCRIPT-OWNS-SCANNING/REFERENCES-CITED must pass. Arms: status law (done needs a failing-capable test; demotion; failing done -> wip), snapshot immutability, born-red proof, adapters (file real; http stub returns reason 'hub contract unverified' and never sends), key mint/check/revoke.
DONE-WHEN: atlas fixture green from a scratch copy; eval rc=0 with 33 skills; the seat can mint a key and `key --check` passes on this machine.
