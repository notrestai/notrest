# 4.7.0 lane H — hooks (KERNEL — refuter round follows)
COMMISSION (Director session 3, banked 2026-09-05T05:16Z before dispatch; owner GO 2026-09-05 for 4.7.0 — the docket is docs/DOCKET-4.7.md, read it first). Repo /Users/ethanabot/Desktop/oracle-suite-plugin, HEAD 21aa5f8 = v4.6.2; the tree carries the in-flight 4.6.3 learnings loop (folds into 4.7.0). LAWS: TOUCH-ONLY absolute; red-first arms; no commits/staging; never CHANGELOG/manifests/HTML/golden list (seat); no consumer plugin flow; no sub-agents; py3.9 stdlib, stock macOS; hooks keep fast paths and fail-open; persistent lane — feedback resumes you; the seat relays cross-lane interfaces, so put your CLI/JSON contracts in your FIRST return line. RETURN <= 60 lines: per item what changed (file:line), red->green counts, fixture totals, live eval/doctor exit codes, anything undone.
TOUCH-ONLY: hooks/** ; arms in skills/eval/scripts/{hooks-stdin,pretool,router}-fixture.sh.
ITEMS: docket A (finish: parse lane S's JSON contract; non-empty excerpt; add-flag command armed against the real index.py) · B-hooks (estate-pulse.sh runs 'compile.py draft --all-ripe' after a fresh scan when the auto marker exists; session-start.sh AUTO-BUILD echo names the drafted slug as a directive to dispatch builder + refuter lanes; both hooks WARN once when a legacy compile/.auto-build exists without the new marker) · C-trigger (completion-gate consumes the widened --triggers result: unverified claims without an open record block) · D (agent-ledger.sh parses a trailing card block in the lane return — headers TESTS/OPEN/FINDINGS/LEARNINGS, checkbox lines — and banks each line via index.py add with source=lane id and evidence=the banked brief path; malformed card → bank nothing, WARN in the COORD-AGENTS row; arm with a real return) · I (spawn-gate.sh reads a 'DONE-WHEN:' or 'CHECK:' block from the Agent prompt and appends its lines to gates/ACTIVE.md under '## lane <id>' (creating the file if absent, never rewriting other sections); agent-ledger.sh retires that section when the lane stops; completion-gate runs them as today; arm: a lane with a failing CHECK blocks the seat's Stop until retired).
DONE-WHEN: hooks-stdin/pretool/router fixtures green from a cp -p scratch copy incl. every new arm; every hook returns inside its stdin bound on an idle stdin; e2e CLI-shape run of each changed hook pasted; live eval rc=0, doctor rc=5 0 fail.
