# Commission — 4.6.3 LEARNINGS LOOP, lane S: SCRIPTS + GATES (establish.py is kernel — refuter round follows)
COMMISSION (Director session 3, banked 2026-09-05T04:52Z before dispatch; owner GO 2026-09-05: 'its a go for me build test and ship as 4.6.3'). Repo: /Users/ethanabot/Desktop/oracle-suite-plugin, HEAD 21aa5f8 = v4.6.2; the tree carries only the refuter-clean eval.py release-surface fix (uncommitted, ships in 4.6.3). FEATURE: THE LEARNINGS LOOP — lessons are banked as validated records, delivered to every session and lane, and audited. Design of record: COORD.md 2026-09-05 04:45Z + 04:47Z lines and this brief.
RECORD (lane S owns the schema, everyone consumes it): archive/findings.jsonl gains kind="learning" with fields: id (L-<n>, assigned by index.py), ts (UTC), tag in {INHERITED,RULED,LEARNED}, statement (<=300 chars), evidence (list, >=1: a COORD timestamp '[YYYY-MM-DD HH:MMZ]', a briefs/ path, a commit hash, or a finding id), scope (list, >=1: a path glob, a skill name, or 'estate'), source ('seat' or a lane id). index.py add refuses a learning missing evidence or scope. index.py gains 'learnings' with --digest (framed '| ' lines, <=200 bytes each, newest first, max N) and --scope <tokens...> (records whose scope matches any token or is 'estate'). DIGEST FORMAT (shared by packet, spawn-gate, router): '| L-<n> [TAG] <statement clipped> — evidence: <first>'.
LAWS: TOUCH-ONLY is absolute; red-first fixture arm for every behavior; no commits/staging; never CHANGELOG/manifests/HTML/golden list (seat); no consumer plugin flow; no sub-agents; py3.9 stdlib, stock macOS; every hook keeps its fast path and fail-open law; you are PERSISTENT — feedback resumes you. RETURN <= 60 lines: per item what changed (file:line), red->green counts, fixture totals, live eval/doctor exit codes, anything left undone.
TOUCH-ONLY: plugins/notrest/skills/archivist/scripts/** ; plugins/notrest/skills/notrest/scripts/establish.py + fixture.sh ; plugins/notrest/skills/eval/scripts/eval.py + fixture.sh ; plugins/notrest/skills/doctor/scripts/doctor.py + fixture.sh.
ITEMS: (1) archivist index.py: the learning kind + validation + 'learnings --digest [--scope ...] [--limit N] [--json]' + 'learnings --since <ts>' (records newer than a ledger timestamp) — arms: add refuses no-evidence/no-scope, accepts a full record, digest lines <=200 B and framed, scope match on path/skill/estate, since filter. (2) establish.py continuation --brief: a LEARNINGS block after the ledger tail — total count + newest 3 digest lines, same framing/byte law as the rest of the packet, absent (not empty) when the store has none; packet still terminates; arms in the notrest fixture incl. the 4.6.0 refuter invariants (no line at column 0, ctrl chars rendered). (3) eval LEARNING-LOOP check: TRIGGER lines = COORD ledger lines matching the shared regex (case-insensitive) 'CORRECTION|owner correct|REFUTER.*(DEFECT|BLOCKER|NOT CLEAN)|\bRED:|HALTED' — for every trigger line with ts >= the ts of the FIRST learning record in the store, a learning record must cite that timestamp in evidence; SKIP 'loop not armed' when the store has no learning; FAIL listing the first 3 uncited triggers; PASS with counts. Put the trigger regex in ONE place index.py can print ('index.py learnings --trigger-regex') so the Stop hook (lane H) and eval cannot drift — arm: eval's regex == index.py's. (4) doctor: an ESTATE detail line 'learnings: N records, newest <age>' (no new status). Also confirm the pre-existing eval.py lexists fix is intact.
DONE-WHEN: archivist, notrest, eval, doctor fixtures green from a scratch copy with the new arms; live eval rc=0 (LEARNING-LOOP SKIP or PASS on this estate — if you bank a first learning to arm it, it must cite a real trigger line), doctor rc=5 0 fail.
