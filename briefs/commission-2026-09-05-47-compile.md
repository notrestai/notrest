# 4.7.0 lane C — compile + watch (new persistent domain lane) · model: opus — tier: judgment
COMMISSION (Director session 3, banked 2026-09-05T05:16Z before dispatch; owner GO 2026-09-05 for 4.7.0 — the docket is docs/DOCKET-4.7.md, read it first). Repo /Users/ethanabot/Desktop/oracle-suite-plugin, HEAD 21aa5f8 = v4.6.2; the tree carries the in-flight 4.6.3 learnings loop (folds into 4.7.0). LAWS: TOUCH-ONLY absolute; red-first arms; no commits/staging; never CHANGELOG/manifests/HTML/golden list (seat); no consumer plugin flow; no sub-agents; py3.9 stdlib, stock macOS; hooks keep fast paths and fail-open; persistent lane — feedback resumes you; the seat relays cross-lane interfaces, so put your CLI/JSON contracts in your FIRST return line. RETURN <= 60 lines: per item what changed (file:line), red->green counts, fixture totals, live eval/doctor exit codes, anything undone.
TOUCH-ONLY: skills/compile/scripts/** , skills/compile/SKILL.md (doctrine section only — docs lane owns prose), skills/watch/scripts/**.
ITEMS: docket B (1: 'auto' WARNs on a legacy compile/.auto-build without a new marker, exit 5 text names the migration; 2: 'compile.py draft --all-ripe' scaffolds every ripe NEW candidate idempotently — contract + skeleton + benchmark harness, zero tokens, records status DRAFTED in decisions.md, never touches an existing slug; the pulse daemon calls it after scan (lane H wires the call — give them the exact command and exit codes); 4: 'decide --status ADOPTED' requires --evidence for fixture, benchmark and refuter receipts, refuses without them, writes the ledger-ready receipt line to stdout) · F (scan reads archive/findings.jsonl learning + open records; a statement recurring >=3 times by normalized text becomes a candidate kind 'rule' in candidates.json/.md with the cited record ids) · G (open records carry 'recheck' dates; 'watch.py due' lists due open records by id; 'watch.py close <id>' appends the closing record via index.py and cites the open id).
DONE-WHEN: compile fixture (169+) and watch fixture green from a scratch copy incl. every new arm; scan runtime stays < 10 s on the seeded corpus; live 'compile.py auto --root .' exit 0 (opted in).

## ADDENDUM 2026-09-05T05:25Z — owner ruling: FULLY UNATTENDED (supersedes "the daemon never spends tokens")
Add `compile.py auto-run [--next | --slug S] [--runner CMD] [--dry-run]`: the pipeline runner the pulse daemon calls. Steps: pick the oldest DRAFTED candidate (or --slug) → BUILD via the runner (default: `claude -p --model opus --output-format json --max-turns N` with the candidate's contract + the compile ritual as the prompt, cwd = compile/<slug>/, env NOTREST_UNATTENDED=1) → REFUTE via a second runner call with the refuter brief → BENCHMARK (existing FAIR harness, script) → ADOPT via `decide --status ADOPTED --evidence ...` only when all three are green; any red → status stays with the reason in decisions.md. Rails, each with a red-first arm using a FAKE runner (a shell script the fixture writes; no real tokens ever spent in tests): (a) estate-wide lock file — a second auto-run exits 0 'busy' immediately; (b) the marker gains `unattended` and `daily_cap_tokens` (`auto --on --unattended --daily-cap N`; `auto` prints both); the runner sums today's daemon receipts from spend/ledger.md and REFUSES a step that would breach the cap, writing one pulse line — arm: fake runner reports 60% of the cap twice → second run refused (11b: prove the cap FIRES, not that it is configured); (c) every runner call is receipted with `spend.py log` (lane=daemon, model explicit, tokens from the JSON result's usage; unknown → tokens=? grade=unverifiable, never invented); (d) runner exit non-zero or stdout containing 'Not logged in' → stop quietly, one pulse line, status unchanged, exit 0; (e) --dry-run prints the plan and spends nothing. Also extend TOUCH-ONLY by plugins/notrest/skills/spend/scripts/spend.py ONLY if `spend.py log` cannot express the daemon receipt as-is (say so; keep spend report CLEAN on the new lane kind). Print auto-run's exact CLI + exit codes in your first return line for lane H (who wires the daemon call).
