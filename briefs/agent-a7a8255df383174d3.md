# lane brief — agent-a7a8255df383174d3

- extracted: 2026-09-01 07:18Z
- agent: a7a8255df383174d3
- model: claude-opus-5
- transcript: /Users/ethanabot/.claude/projects/-Users-ethanabot-Desktop-oracle-suite-plugin/c659cb78-4fd1-4c39-bdcd-2258e8998e4b/subagents/agent-a7a8255df383174d3.jsonl

Auto-extracted by the notrest SubagentStop hook: the first user-role
message of the agent transcript — the exact prompt the seat passed to
this lane. Reproduced verbatim below; never edited, never summarized.

---

You are the MAP lane for notrest v4.5.0 (just shipped, bd9fec0). Repo: /Users/ethanabot/Desktop/oracle-suite-plugin. Produce the COMPLETE plugin map: every hook, every instrument, every skill — each explained SIMPLY, evidence-based not recalled. Write the deliverable to docs/MAP.md (you may write ONLY that one file). The audience: someone smart who has never seen the plugin — the owner will use this for a gap review, then a workshop.

METHOD — probe, don't recall:
1. HOOKS (all 11 in plugins/notrest/hooks/ + hooks.json): for each — which lifecycle event fires it, what it does in ONE plain sentence, what it writes (or "echo only"), and its one safety property (silent-on-failure, fail-open, containment…). Read each script's header comments; they are honest.
2. INSTRUMENTS (every .py/.sh under skills/*/scripts/ that is a runnable tool, not a fixture): for each — run its --help or bare invocation where read-only-safe (doctor check, eval check, spend report, graph/cockpit status, compile report, establish check --root ., watch due, mentor checkpoints --help, gate-check --help, walk.py --help, swarm.py --help etc. — NEVER run anything that writes, spawns, or spends), and give: what question it answers, in one sentence + its exit-code grammar if it has one. Group by the skill that owns it.
3. SKILLS (all 32): ONE simple sentence each — what you'd say to a stranger. You may derive from each SKILL.md's own frontmatter description (read them; do not invent). Mark the 9 harness-spine skills (notrest, oracle, sessionend, fable-mode, doctor, eval, recap, graph, agentswarm) distinctly from the knowledge verbs and arrangements.
4. THE ESTATE FILES: one short section — COORD.md, COORD-AGENTS.md, spend/ledger.md, briefs/, archive/findings.jsonl, gates/ACTIVE.md, pulse/, ~/.notrest/auto-build — each one line: who writes it, who reads it.
5. A closing "how it all fits" paragraph: the bookends (oracle/sessionend), the posture (fable-mode), the trail, the gates, delegation, continuity — 10 lines max, plain words.

STYLE: plain language, short sentences, no marketing. Every claim about behavior must come from a file you read or a command you ran this session. If something surprises you or looks missing/inconsistent, add it to a final "## Loose threads found while mapping" section — the owner explicitly wants gap candidates.

VERIFY before returning: the file exists, covers 11 hooks + 32 skills (count them in your own output), and every instrument you name exists on disk. Return tight: the section counts, any loose threads found (verbatim), and nothing else — the map itself lives in the file.
