# Commission — 4.6.2 REFUTER ROUND (kernel law: a kernel change ships only through a refuter round)
Banked 2026-09-02T00:10Z by the Director (session 3) BEFORE dispatch. model: opus — tier: judgment (adversarial review of kernel surfaces).
Repo: /Users/ethanabot/Desktop/oracle-suite-plugin. Baseline HEAD 54cb763 = v4.6.1. The working tree holds the un-committed 4.6.2 build: 'git diff HEAD --stat' is your target; the intent is docs/DOCKET-4.6.2.md + briefs/amendment-2026-09-01-offload-policy.md + briefs/commission-2026-09-01-build-462-{hooks,scripts,docs}.md.
ROLE: find-only. You modify NOTHING (worker never fixes); scratch copies under /private/tmp/claude-501/-Users-ethanabot-Desktop-oracle-suite-plugin/530a1524-2861-4fec-a687-2c01420a1a15/scratchpad/refuter/ only. Never run the CLI's consumer plugin install/update/marketplace flow. Never write ledgers/briefs. No sub-agents.
KERNEL SURFACES IN THIS DIFF (attack these first, hardest): plugins/notrest/hooks/** (stdin guards, per-event timeouts, banner fallback, completion-gate empty-stdin path, mode changes); plugins/notrest/skills/notrest/scripts/establish.py (protocol v2->v3 bump, BODY_V3 offload text, upgrade-in-place path, hand-edit detection); plugins/notrest/skills/recap/scripts/walk.py (ledger consumer: STAMP-ORDER row); plugins/notrest/skills/doctor/scripts/doctor.py (ROSTER PARITY, RENDER count, GITIGNORE tracked-derived); plugins/notrest/skills/eval/scripts/eval.py (OFFLOAD-POLICY TIER_RE widening — can a haiku/fork/omitted-model sentence now slip past? can a 'sonnet for everything' surface pass?); plugins/notrest/skills/compile/scripts/compile.py (memoized wjaccard — is the identity exact for every weight type it sees? does the cache survive agglomerate's merges correctly?).
ATTACKS REQUIRED (each with a runnable repro and observed output): (1) stdin: hook with idle open stdin (must not hang), with a payload larger than one pipe buffer (64 KiB+) arriving at once (must read all of it — prove by a payload whose deny-trigger sits in the LAST bytes), with a payload delivered in two writes 1s apart (what happens?), with EOF-without-newline; (2) timeouts: does any hooks.json timeout cut a legitimately slow hook (session-start on a 900MB ledger, agent-ledger on a 1000-line COORD-AGENTS roll)? (3) establish: a v2 estate with a hand-edited block, a v2 estate with the marker but a truncated block, an estate with BOTH CLAUDE.md and AGENTS.md at different protocol versions, a read-only CLAUDE.md, a symlinked CLAUDE.md escaping the root; (4) doctor: a README whose skill table names a skill in a code span or with different casing, a marketplace description that names 'mentor' only inside another word, an HTML with two different N-skill phrases; (5) eval: craft the smallest SKILL.md sentence that names haiku as lawful yet passes OFFLOAD-POLICY under the new TIER_RE; (6) compile: a corpus where two signatures share every token (jaccard 1) and one where weights are zero — same candidates.json before/after the memoization (use 'git show HEAD:plugins/notrest/skills/compile/scripts/compile.py' as the oracle); (7) fixtures: pick any three new arms and make each FAIL on purpose by reverting its fix — an arm that stays green is a finding.
RETURN (<= 80 lines): FINDINGS table — severity (BLOCKER = ships a defect / DEFECT = wrong but bounded / NIT) · file:line · one sentence · REPRO command + observed output · [verified]; then the attacks that FAILED to find anything, named positively; then what you could not attack and why.
DONE-WHEN: all seven attack classes appear by number in FINDINGS or the clean list.
