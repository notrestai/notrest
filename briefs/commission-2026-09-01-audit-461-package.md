# Commission — 4.6.1 audit lane A: PACKAGE & INSTALL SURFACE
COMMISSION (Director session 3, banked 2026-09-01T21:26Z before dispatch). Repo: /Users/ethanabot/Desktop/oracle-suite-plugin, HEAD 54cb763 = v4.6.1. Package under audit: plugins/notrest (32 skills, 13 hooks, 58 scripts).
TOUCH-ONLY: NOTHING inside the repo may be modified — this is a find-only audit (worker never fixes). Scratch work only under /private/tmp/claude-501/-Users-ethanabot-Desktop-oracle-suite-plugin/530a1524-2861-4fec-a687-2c01420a1a15/scratchpad/<lane>/ (make a copy of the tree there if a fixture needs a writable root).
HARD BANS: never run the CLI's consumer plugin flow on this machine — no plugin install, no plugin update, no marketplace add/update subcommands (the installed copy shadows the live skills-dir runtime — proven twice; the pretool gate will refuse and you must not override it). Never write to spend/ledger.md, COORD.md, COORD-AGENTS.md, briefs/. Never spawn sub-agents.
RETURN SHAPE (tight, <= 80 lines): (1) FINDINGS table — severity (ERROR = broken behavior / FIX = wrong but working / MISSING = promised but absent) · file:line · one-sentence defect · REPRO: one exact command and its observed output/exit code · label [verified]/[unverified]. (2) CLEAN list — every check you ran that found nothing, named positively (an absence is only evidence when the check is recorded). (3) Anything you could not check and why. No prose beyond that.
SCOPE: the consumer-facing package. Check: (a) plugins/notrest/.claude-plugin/plugin.json vs .claude-plugin/marketplace.json — version parity, name parity, description skill counts ('thirty-two') vs the real skills dir; the oracle-suite tombstone entry pinned 9.0.0 and its source dir intact; (b) plugins/notrest/hooks/hooks.json — every referenced hook file exists, 'bash -n' clean, and each RUNS silently under realistic conditions: empty stdin, absent stdin, a minimal JSON payload of its event type, CLAUDE_PLUGIN_ROOT set and unset, cwd outside any estate — a hook must exit 0 and print nothing hostile; report any stderr/stdout leakage, non-zero exit, or dependency on a tool absent from a stock macOS (e.g. timeout, gdate, python3 version assumptions); (c) every skills/*/SKILL.md front-matter parses (name, description present, description length, no YAML breakage), skill dir name matches front-matter name; (d) README.md skill table vs the 32 dirs (rows missing/extra/stale versions); docs/oracle-skill-flow.html version stamps; (e) the CLI's read-only plugin 'list' and 'validate' subcommands if offered (read-only only; if the gate refuses, record that and move on); (f) stray/undocumented dirs inside the package (evals-legacy-external-runner, graph, docs) — are they referenced, are they needed, are they gitignored where they should be; (g) file modes (scripts executable?), symlinks, files > 1MB, secrets-looking strings.
DONE-WHEN: return shape satisfied with a repro per finding; sections (a)-(g) each appear in FINDINGS or CLEAN by letter.
