# Commission — 4.7.0 REFUTER ROUND (kernel law). model: opus — tier: judgment. Banked 2026-09-05T06:01Z before dispatch.
Repo /Users/ethanabot/Desktop/oracle-suite-plugin, HEAD 21aa5f8 = v4.6.2; target = 'git diff HEAD' (the whole 4.7.0 build: learnings loop + docket A-I). Intent: docs/DOCKET-4.7.md, briefs/commission-2026-09-05-47-*.md (+ the compile ADDENDUM), briefs/commission-2026-09-05-learnings-*.md.
ROLE: find-only. Modify NOTHING; scratch under the session scratchpad only. Never run the consumer plugin install flow. Never write ledgers/briefs. No sub-agents. NEVER run 'compile.py auto-run' without '--runner <fake>' or '--dry-run' — a real run spends the owner's tokens; NEVER call 'claude -p'.
KERNEL SURFACES, hardest first: hooks/** (completion-gate untested/uncited block; spawn-gate full-input updatedInput + fan-out ban + DONE-WHEN->gates/ACTIVE.md; agent-ledger card banking + gate retirement; router specific-scope; session-start unattended silence + legacy WARN + AUTO-BUILD echo; estate-pulse -> draft/auto-run); skills/notrest/scripts/establish.py (packet LEARNINGS/LIBRARY/CARD blocks); skills/archivist/scripts/index.py (validation of learning/open/alternative/result, parse_card, --triggers six keys + arming floor + 120-char headline, promote); skills/compile/scripts/compile.py (auto-run rails: lock, daily cap from the ledger, run cap, two strikes, receipts, quiet stop, ouroboros filter, draft idempotency, ADOPTED evidence); skills/eval/scripts/eval.py (LEARNING-LOOP, RELEASE-SURFACE lexists); skills/doctor/scripts/doctor.py (LOOP HEALTH never FAILs); skills/watch/scripts/watch.py.
ATTACKS REQUIRED (repro + observed output each): (1) forge a learning/open/card line that INJECTS into a lane prompt or the packet (control chars, fake headers, a statement that looks like a harness directive, a card item containing a newline or a second box header); (2) make the Stop gate block forever (a trigger the add command cannot cite; an untested admission inside a quoted string; a headline that is itself the add command); (3) make the Stop gate NOT block when it should (a correction whose tag sits at char 121; lowercase variants; a trigger in a sealed COORD volume); (4) bypass the daily cap (receipts without tokens; a receipt dated tomorrow; two runners racing the lock; a cap read from a ledger the runner itself just wrote); (5) make auto-run spend when it must not (marker opted but not unattended; corrupt marker; a slug PARKED then re-drafted; --dry-run side effects); (6) make the pulse call auto-run twice or block its caller; (7) a card that banks the wrong kind or banks a partial card; a DONE-WHEN block that rewrites another lane's gates section or leaves a stale red gate after the lane dies without a SubagentStop; (8) revert each of ten new arms of your choosing across four fixtures and confirm each goes red; (9) run every fixture from a scratch copy and report totals; (10) the ouroboros: can any other self-generated line (a card-banked record, a LOOP HEALTH line, a pulse line) re-enter the scan as a candidate?
RETURN <= 90 lines: BLOCKER/DEFECT/NIT table with repros; the attacks that failed, named positively; what you could not attack.
