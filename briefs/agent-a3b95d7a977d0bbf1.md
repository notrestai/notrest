# lane brief — agent-a3b95d7a977d0bbf1

- extracted: 2026-09-05 06:00Z
- agent: a3b95d7a977d0bbf1
- model: claude-opus-5
- transcript: /Users/ethanabot/.claude/projects/-Users-ethanabot-Desktop-oracle-suite-plugin/530a1524-2861-4fec-a687-2c01420a1a15/subagents/agent-a3b95d7a977d0bbf1.jsonl

Auto-extracted by the notrest SubagentStop hook: the first user-role
message of the agent transcript — the exact prompt the seat passed to
this lane. Reproduced verbatim below; never edited, never summarized.

---

model: opus — tier: judgment (an unattended pipeline runner with spend rails, a scanner that must stay fast, and a scheduler over records). You are the persistent COMPILE + WATCH lane for notrest v4.7.0. Read FIRST, in order: /Users/ethanabot/Desktop/oracle-suite-plugin/briefs/commission-2026-09-05-47-compile.md INCLUDING its ADDENDUM (owner ruling: fully unattended), docs/DOCKET-4.7.md items B, F, G, then plugins/notrest/skills/compile/SKILL.md, scripts/compile.py (4.6.2 memoized scan; the §M 10 s runtime arm in its fixture), skills/watch/scripts/watch.py + fixture, and plugins/notrest/skills/spend/scripts/spend.py --help (the `log` verb is how you receipt a headless run). TOUCH-ONLY: skills/compile/scripts/**, skills/compile/SKILL.md doctrine section only, skills/watch/scripts/**, and spend.py only if `log` cannot express a daemon receipt (say so). Deliver: `auto` warns on a legacy in-repo marker; `auto --on --unattended --daily-cap N` and `auto` printing both; `draft --all-ripe`; `auto-run [--next|--slug] [--runner CMD] [--dry-run]` with the lock, the cap that is PROVEN to fire under a fake runner, spend receipts from the CLI JSON usage (tokens=? + unverifiable when absent, never invented), quiet stop on non-zero/"Not logged in", NOTREST_UNATTENDED=1 on every run; `decide --status ADOPTED` requiring fixture/benchmark/refuter evidence; scan reading learning + open records so a statement recurring >=3 times becomes a `rule` candidate; `watch.py due`/`close <id>` over open records (lane S owns index.py and is defining the `open` schema now — read archive/findings.jsonl and index.py's validator, build to what exists, and flag any gap). Fixtures never spend a real token: every runner arm uses a fake runner script the fixture writes. Red-first arms for every behavior; scan under the 10 s bound; py3.9 stdlib; no commits, no CHANGELOG/manifests, no consumer plugin flow, no sub-agents. Put the exact CLI + exit codes of every verb you add in your FIRST return line so the seat can wire the pulse daemon (lane H). Return under 60 lines in the brief's shape.

[notrest LEARNINGS — banked lessons in scope; read before acting]
| L-4 [LEARNED] Never resolve an ambiguous ask from what the estate already contains; ask the owner the FORM of a deliverable before building, and a release needs its own explicit ask every time. Disc
