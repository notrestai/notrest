# lane brief — agent-ae8a75b9573043740

- extracted: 2026-09-01 06:21Z
- agent: ae8a75b9573043740
- model: claude-opus-5
- transcript: /Users/ethanabot/.claude/projects/-Users-ethanabot-Desktop-oracle-suite-plugin/c659cb78-4fd1-4c39-bdcd-2258e8998e4b/subagents/agent-ae8a75b9573043740.jsonl

Auto-extracted by the notrest SubagentStop hook: the first user-role
message of the agent transcript — the exact prompt the seat passed to
this lane. Reproduced verbatim below; never edited, never summarized.

---

You are the persistent BUILDER lane for domain A of notrest v4.5.0. Repo: /Users/ethanabot/Desktop/oracle-suite-plugin. TOUCH ONLY: plugins/notrest/skills/notrest/scripts/establish.py and plugins/notrest/skills/notrest/scripts/fixture.sh. Do NOT commit or push; the seat gates. Read docs/DOCKET-4.4.md items 1-6 FIRST — they carry the evidence pointers; the refuter transcripts' repro commands are quoted there.

BUILD, all six, each with fixture arms WATCHED RED before the fix lands (run the arm against the unmodified code first, or mutation-prove; show which):

1. F2 ruling (a), owner-ratified: detect_surface — HOST SIGNALS decide the runtime; files may only narrow WITHIN a detected host; no signal → claude (the stated historical default, now actually enforced). Fix the signal list to variables that exist: Claude = any of CLAUDE_PID / CLAUDECODE / CLAUDE_CODE_* (probe `env | grep ^CLAUDE` yourself and use what is real); Codex = CODEX_THREAD_ID / CODEX_SANDBOX (unverifiable here — keep, mark the honest comment). THE WRITE GUARD (option b folded in): `establish` under auto never CREATES a surface file for an undetected host — reads/checks may grade whatever files exist (read-only honesty), but creating AGENTS.md requires a codex signal or an explicit --surface, and CLAUDE.md likewise for claude (no-signal default claude may still create CLAUDE.md — that IS the default). Explicit --surface always wins, unchanged.
2. F3: the report must not escape the root — foundation_state() resolves through the same containment the writers use (an escaping symlink foundation is a FAIL finding, never a PASS read from outside), and grade() consults failures: a run with a refused write can no longer print ESTABLISHED exit 0, and the `wrote:` tail must tell the truth about partial writes.
3. F4: atomic_write preserves the target's mode (stat before, chmod the tmp to match before replace); a read-only target is REFUSED with an honest message instead of rewritten; and no message claims "untouched" when the file object changed.
4. Case-variant bypass: rework the home-refusal family (HOME, /, Desktop/Documents/Downloads, dot-dirs) onto inode identity — os.path.samefile guarded for existence — so /users/ vs /Users/ and aliased ancestors cannot dodge the compare; keep the lexical dot-dir leg (leaf-as-named) from 4.4.1. The docket cites the review lane's repro commands.
5. F5: fixture.sh:~103 "Codex runtime variable selects Codex automatically" asserts only exit 0 — re-point it at the property (which file was written), like its neighbours.
6. F6: clone the F-12(a) hostile corpus for AGENTS.md: CRLF+latin-1, fenced-example markers, duplicate/unterminated markers, escaping symlink, read-only file, UTF-16 refusal, and a partial `both` run — mirroring the CLAUDE.md arms.

CONSTRAINTS: python3 stdlib only; every write atomic+idempotent; byte-exact round trips preserved; the REPORT/JUDGE split holds (adoption facts never move the exit code); existing 278 fixture arms stay green except where an arm asserted the OLD defective contract — bring those to the new contract with a comment naming the docket item.

VERIFY before returning (exit codes direct, never | tail): py_compile 0; full fixture green with the new arm count; for each item, the docket's own repro command now behaving per the new contract (paste before/after). RETURN tight: per-item — what landed, the watched-red proof, the repro before/after; files +/- counts; anything not done and why; scope drift disclosed first.
