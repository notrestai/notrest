# lane brief — agent-ac2505bf7898b9f71

- extracted: 2026-07-27 02:54Z
- agent: ac2505bf7898b9f71
- model: claude-opus-5
- transcript: /Users/ethanabot/.claude/projects/-Users-ethanabot-Desktop-oracle-suite-plugin/97c59e4b-e7d2-4827-b4bf-2b918d5221d2/subagents/agent-ac2505bf7898b9f71.jsonl

Auto-extracted by the notrest SubagentStop hook: the first user-role
message of the agent transcript — the exact prompt the seat passed to
this lane. Reproduced verbatim below; never edited, never summarized.

---

BUILDER lane 4 of 6, domain: GRAPH (journey gaps G7-river/G18/G11-part). Repo: /Users/ethanabot/Desktop/oracle-suite-plugin (HEAD 204d2f3, v3.8.0). Working tree only; NO commits, NO bumps. FIVE sibling lanes edit OTHER dirs — TOUCH ONLY: plugins/notrest/skills/graph/ (scripts/graph.py, scripts/river-fixture.sh or a new journey fixture, SKILL.md). Live-loaded tree; end-only instruments; NEVER touch the front-matter description — body + scripts only.

THREE BUILDS:

1. G7 (river half) — RESTS-ON-REFUTED painted. RULE PINNED (a sibling implements the same rule in archivist track — identical semantics): a live record whose links contain an effectively-refuted id rests on refuted ground. In river: that stone's INBOUND edge(s) from the refuted source render red (distinct from rock-red: use a dashed red edge + a legend entry "rests on refuted"), and the hover card says RESTS-ON-REFUTED F-<id>. Compute it from the store yourself with the existing link-walk — do not depend on the sibling's track output. Fixture: extend river-fixture.sh — seed A(finding) ← tombstone-refuted, B(decision links A) live → assert edges json marks the A→B edge rests_on_refuted:true, legend present; control pair unflagged.
2. G18 — the journey render. graph.py gains `journey [--root .] [--out graph/journey.html] [--no-open]`: reads plugins/notrest/hooks/router.sh (the SKILL=/SHAPE= case arms — same parse eval uses), plugins/notrest/skills/oracle/SKILL.md's routing bullet, and each skill's chain/hand-off lines (grep for the Chains/finishing-up section headers; best-effort, disclose what you could not parse), then renders a self-contained flow page: user-phrase pills → shape → skill node → chains-to arrows; skills with no route drawn in a "by name only" band. Same laws as river: deterministic (sorted, no wall-clock — stamp from newest input mtime is FORBIDDEN too since mtimes vary by clone: stamp from git HEAD short-hash via `git rev-parse --short HEAD`, disclose if git absent), zero external assets, both themes, ≤200KB, hand-rolled SVG/CSS. Fixture: run against the real repo — assert ≥14 shape nodes, 28 skill nodes, byte-identical double render, render-check.sh HTTP 200, both theme hooks, size cap.
3. G11 (graph side) — cut the verbatim phrase `how did we get here` from graph/SKILL.md trigger text (recap owns it now; a sibling adds the router arm). Keep graph's other triggers intact.
4. G9 body line: graph/SKILL.md → **Router shape:** `project graph` — wait, the arm a sibling adds for graph is shape `file-graph` with phrase "project graph". Write exactly: **Router shape:** `file-graph` — format `**Router shape:** \`<shape>\`` on its own line near the trigger section.

SELF-CHECK at end: both graph fixtures (report counts); render-check receipts; eval.py + doctor.py once each — report exits, weigh only your-file findings (siblings cause transients; your fixture must not break on their store/schema edits — you own no schema).

RETURN (tight): graph.py line delta; journey.html size + node counts on the real repo; the rests-on-refuted edge json sample; fixture counts; instrument exits; deliberately-not-done.
