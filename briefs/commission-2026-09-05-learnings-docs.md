# Commission — 4.6.3 LEARNINGS LOOP, lane D: DOCS + SKILL TEXT (no refuter; seat gates by eval/doctor/greps)
COMMISSION (Director session 3, banked 2026-09-05T04:52Z before dispatch; owner GO 2026-09-05: 'its a go for me build test and ship as 4.6.3'). Repo: /Users/ethanabot/Desktop/oracle-suite-plugin, HEAD 21aa5f8 = v4.6.2; the tree carries only the refuter-clean eval.py release-surface fix (uncommitted, ships in 4.6.3). FEATURE: THE LEARNINGS LOOP — lessons are banked as validated records, delivered to every session and lane, and audited. Design of record: COORD.md 2026-09-05 04:45Z + 04:47Z lines and this brief.
RECORD (lane S owns the schema, everyone consumes it): archive/findings.jsonl gains kind="learning" with fields: id (L-<n>, assigned by index.py), ts (UTC), tag in {INHERITED,RULED,LEARNED}, statement (<=300 chars), evidence (list, >=1: a COORD timestamp '[YYYY-MM-DD HH:MMZ]', a briefs/ path, a commit hash, or a finding id), scope (list, >=1: a path glob, a skill name, or 'estate'), source ('seat' or a lane id). index.py add refuses a learning missing evidence or scope. index.py gains 'learnings' with --digest (framed '| ' lines, <=200 bytes each, newest first, max N) and --scope <tokens...> (records whose scope matches any token or is 'estate'). DIGEST FORMAT (shared by packet, spawn-gate, router): '| L-<n> [TAG] <statement clipped> — evidence: <first>'.
LAWS: TOUCH-ONLY is absolute; red-first fixture arm for every behavior; no commits/staging; never CHANGELOG/manifests/HTML/golden list (seat); no consumer plugin flow; no sub-agents; py3.9 stdlib, stock macOS; every hook keeps its fast path and fail-open law; you are PERSISTENT — feedback resumes you. RETURN <= 60 lines: per item what changed (file:line), red->green counts, fixture totals, live eval/doctor exit codes, anything left undone.
TOUCH-ONLY: plugins/notrest/skills/archivist/SKILL.md ; plugins/notrest/skills/fable-mode/SKILL.md ; plugins/notrest/skills/agentswarm/SKILL.md ; plugins/notrest/skills/notrest/SKILL.md ; plugins/notrest/skills/doctor/SKILL.md ; plugins/notrest/skills/eval/SKILL.md ; plugins/notrest/skills/sessionend/SKILL.md ; README.md ; plugins/notrest/README.md ; docs/MAP.md (content; the seat stamps the version line) ; docs/CAPABILITIES.md ; docs/TUTORIAL.md ; docs/workshop/** ONLY for count/version claims that 4.6.3 changes (doctor/eval check counts, packet shape) — nothing else in the pack.
ITEMS: (1) archivist SKILL.md documents the learning kind, the three tags, evidence/scope rules, the 'learnings' verb and digest format; (2) fable-mode gains the rule 'bank the lesson' (a correction, a refuter finding or a red gate is a trigger; the Stop gate enforces it; INHERITED/RULED/LEARNED tags) in Hard Rules, tight; (3) agentswarm: the spawn gate injects a scoped LEARNINGS digest into every lane prompt — say so in the model-policy/brief section, and instruct lanes to cite a learning id when they act on one; (4) notrest SKILL.md: the packet's LEARNINGS block; (5) doctor/eval SKILL.md check lists (LEARNING-LOOP; the ESTATE detail); (6) sessionend: the handoff names the newest learnings; (7) README/plugin README/MAP/CAPABILITIES/TUTORIAL: one honest paragraph each where the estate or the hooks are described; MAP maps the new verb, check, packet block and hook rules; (8) sweep every golden doc for check counts (doctor 12 -> as landed, eval 15 -> 16) and packet-shape claims and correct them, printing the command beside any number that can drift. Verify each claim against the tree at the END of your run (lanes S and H are landing concurrently; describe only what exists).
DONE-WHEN: eval rc=0 (incl. OFFLOAD-POLICY and RELEASE-SURFACE), doctor rc=5 0 fail, starthere_lint rc=0 if you touch START-HERE, every command you cite run as written.
