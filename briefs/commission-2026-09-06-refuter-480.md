# Commission — 4.8.0 REFUTER ROUND (kernel law). model: opus — tier: judgment. Banked 2026-09-06T08:43Z before dispatch.
Repo /Users/ethanabot/Desktop/oracle-suite-plugin, HEAD afe2270 = v4.7.1; target = 'git diff HEAD' + the new untracked skills/atlas, hooks/atlas-bank-hook.sh, .access/keys.sha256, plugins/notrest-free-tombstone. Intent: docs/DOCKET-4.8.md + briefs/commission-2026-09-06-48-*.md.
ROLE: find-only; modify NOTHING; scratch only; never the consumer plugin flow; never claude -p or a real auto-run; never print, copy or move the seat's real key (~/.notrest/access-key) — mint your own in a scratch keyring; no ledger/brief writes; no sub-agents.
KERNEL SURFACES, hardest first: hooks/estate-root.sh (the shell key verifier: hash compare, cache under pulse/, parity with atlas.py key --check), hooks/session-start.sh (keyless one-line banner, nothing else), every other hook keyless (silent, deny rules still deny), skills/notrest/scripts/establish.py (exit 7, zero-byte continuation, --atlas wiring), skills/atlas/scripts/atlas.py (key mint/check/revoke, the status law derive(), snapshot immutability, adapters, wire/--prove/--unwire, status), hooks/atlas-bank-hook.sh (fast paths, exit 0, re-entrancy), skills/eval/scripts/eval.py ACCESS-GATE, doctor ACCESS KEY.
ATTACKS REQUIRED (repro + observed output): (1) bypass the key: a key file with trailing whitespace/CRLF, a key in env AND a bad file, a keyring with a blank/comment-only line, a hash line with a wrong date, a revoked line re-added by hand, a symlinked key file, the per-session verdict cache reused after revocation (cache poisoning: is the cached verdict keyed to the keyring hash?), TOCTOU between the shell verifier and atlas.py; (2) parity: craft a key the shell path accepts and atlas.py rejects or vice versa (case, whitespace, non-ASCII, length); (3) silence: any keyless hook that writes a byte (drive all 11 with real payloads and the key absent), the banner exceeding one line, and a keyless machine where the deny rules go quiet; (4) the bank: a gate whose CHECK is 'true'/':'/'exit 0' claimed done (must be demoted), a done that fails (wip+failing), a snapshot rewrite attempt, a board collector that hangs (must say stale, not fail the bank), the http adapter sending anything (grep for sockets/urllib/curl), the post-commit hook re-entering itself, a foreign post-commit hook being clobbered by wire, --prove on the real repo (must refuse or use a scratch clone only), status on an estate with no atlas/; (5) the tombstone: its SessionStart echo tag, exit 0 always, no state; (6) revert eight new arms across the atlas, notrest, eval, doctor and hooks fixtures and confirm each goes red; (7) run every fixture from a scratch copy and report totals.
RETURN <= 90 lines: verdict line CLEAN/NOT CLEAN first; BLOCKER/DEFECT/NIT table with repros; attacks that failed, named positively; could-not-attack.
