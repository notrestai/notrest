# lane brief — agent-a0217a5c89f2535a4

- extracted: 2026-07-27 04:53Z
- agent: a0217a5c89f2535a4
- model: claude-opus-5
- transcript: /Users/ethanabot/.claude/projects/-Users-ethanabot-Desktop-oracle-suite-plugin/97c59e4b-e7d2-4827-b4bf-2b918d5221d2/subagents/agent-a0217a5c89f2535a4.jsonl

Auto-extracted by the notrest SubagentStop hook: the first user-role
message of the agent transcript — the exact prompt the seat passed to
this lane. Reproduced verbatim below; never edited, never summarized.

---

Persistent BUILDER lane, new domain: RIG — the product spec for rig.rest. Repo: /Users/ethanabot/Desktop/oracle-suite-plugin (HEAD 5fed263, v3.14.0, 29 skills). Working tree only; NO commits, NO bumps. TOUCH ONLY: docs/RIG.md (new). This is a SPEC, not code — the build starts in a fresh session from this document.

OWNER VISION (2026-07-27, verbatim intent — quote it at the top of the spec): "rig.rest is a portal I own: the portal you sign into, and you set up your harness — connect models (Claude and ChatGPT for the first build), then you work inside the harness: its own features like Claude Code with the monitoring and graph and all the features happening inside it. The chatroom core value becomes a reality — you can set up your ChatGPT and Claude to work on an idea. If you want to continue mid-session with another model, it is easy — all the COORD and methods we use load up the new model. And the many other things we built."

MATERIAL (read these; the spec cites them): docs/CAPABILITIES.md (the register + four vectors + library doctrine), docs/JOURNEY.md, CHANGELOG.md top 10 entries (v3.6.0→v3.14.0 — one day), plugins/notrest/skills/graph/scripts/cockpit.py + its SKILL.md section (the UI seed), plugins/notrest/skills/chatroom/ (the multi-model wire + room.py secret screen), plugins/notrest/skills/sessionend/ + oracle/ (the continuity/boot machinery — this IS the mid-session model-switch mechanism), plugins/notrest/skills/beam/ (harness carriage on a ref = the tenancy/transport seed), archive/findings.jsonl F-8 (the decision record with the hinge).

WRITE docs/RIG.md (~300-400 lines):
1. THE VISION — owner's words, then one paragraph: the plugin was the spec and test-bed; rig.rest is the shell that owns the loop.
2. THE PORT MAP — a table: every harness asset → its rig component → ports as-is / adapts / stays. (The estate laws+ledgers+store+library, the instruments, router, hooks-as-policy, the cockpit as the UI seed, chatroom as the multi-model room, sessionend/oracle as the model-switch, beam as estate transport, spend as the billing meter, briefs as the transparency surface.) Be honest about the divide: everything below the loop is files+stdlib and ports; the loop, auth, UI shell, connectors are new.
3. ARCHITECTURE (v0.1, LOCAL-FIRST per the F-8 hinge): the rig shell = cockpit grown up (one app: session panel + pictures + feeds); Claude loop via claude-agent-sdk (the laws injected as system context, estate mounted, skills as tools — cite that everything load-bearing is exit-code scripts by design); ChatGPT side v0.1 = OpenAI API bridge promoted from room.py's codex pattern (BE HONEST about the boundary: API keys/official APIs only — no consumer-account automation; name the ToS line plainly); estates = per-project dirs exactly as today; BYO keys; single tenant; loopback.
4. THE THREE SIGNATURE FLOWS, specced concretely: (a) two-model room on an idea (Claude session + GPT bridge in one room, estate recording both, river drawing both); (b) mid-session model continuation (the "switch model" button = sessionend bank → model-agnostic START-HERE/CHECKPOINT → boot the other model's loop with the estate — cite that the continuity files were model-agnostic by design and this session itself cold-booted from them); (c) always-on monitoring (the cockpit panels, already live).
5. v0.2/v0.3 STAGING: hosted rig.rest (auth/tenancy — beam's harness-carriage generalizes to per-user estates; the shadow/UI lessons as product requirements: the runtime always announces itself, commissions always visible), team rooms, the library as a shared org knowledge plane (concepts/crowns across users), marketplace of harness laws.
6. WHAT IS GENUINELY NEW (no self-deception): auth/portal, session-loop hosting, OpenAI connector, UI shell beyond the cockpit, key management, packaging. Rough effort tiers per piece (S/M/L), no dates.
7. RISKS + LAWS THAT TRAVEL: the owner's core values as PRODUCT requirements (transparency: briefs visible in-UI; honesty chips; window-not-control-panel default for observers; the no-secrets screen at every cross-vendor boundary; zero-token renders).
8. KICKOFF PAYLOAD: the exact fresh-session boot block (repo, docs to read in order, the first three build tasks of v0.1 shell).

STYLE: repo voice; laws named; zero hype — this is an engineering spec of things that exist plus an honest list of things that don't.
SELF-CHECK: every cited path/skill/record must exist (verify); eval/doctor untouched-tree check (you add one file; instruments should stay green — run both, report exits).
RETURN (tight): section list with line counts, the port-map table's ports-as-is count vs new-build count, the three flows in one line each, instrument exits, deliberately-not-done.
