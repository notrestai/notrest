# 4.8.0 lane D2 — license, manifests' license field, docs, tombstone
COMMISSION (Director session 3, banked 2026-09-06T08:13Z before dispatch; owner GO 2026-09-06 'repo is private now, go for 4.8'). Repo /Users/ethanabot/Desktop/oracle-suite-plugin, HEAD afe2270 = v4.7.1, origin PRIVATE. Docket: docs/DOCKET-4.8.md (read first). LAWS: TOUCH-ONLY absolute; red-first arms; no commits/staging; never CHANGELOG/manifest versions/HTML/golden list (seat); no consumer plugin flow; no sub-agents; never claude -p or a real auto-run; py3.9 stdlib, stock macOS; hooks keep fast paths + fail-open; persistent lane — feedback resumes you; put every CLI/file contract in your FIRST return line. RETURN <= 60 lines.
TOUCH-ONLY: LICENSE; the "license" field ONLY in plugins/notrest/.claude-plugin/plugin.json, plugins/notrest/.codex-plugin/plugin.json and .claude-plugin/marketplace.json (never the version fields); the marketplace notrest DESCRIPTION; README.md; plugins/notrest/README.md; docs/TUTORIAL.md; docs/CAPABILITIES.md; docs/MAP.md content; skills/atlas/SKILL.md prose only after lane A lands; plugins/notrest-free-tombstone/** (new marketplace entry dir modeled on plugins/oracle-suite-tombstone, pinned 4.7.1 — the seat adds the marketplace entry).
ITEMS: docket C exactly: LICENSE = Not Rest Inc. proprietary, all rights reserved, licensed to Atlas customers under the Atlas terms, no redistribution, with a paragraph stating versions through v4.7.1 were released under MIT and remain so (draft it clearly marked "DRAFT — owner to review"); manifests' license "Proprietary"; marketplace description opens "Part of Atlas — …"; every install section: private repo + owner-issued access key (~/.notrest/access-key) + `/plugin marketplace add notrestai/notrest` works only with GitHub access, then the bank; remove every "free" claim (grep -i free across the touched files must leave only the MIT-history sentence); tombstone entry text tells a 4.7.1 install that the harness is part of Atlas and how to ask for access. Verify every command against the tree at the end; describe only what landed.
DONE-WHEN: eval rc=0 (ROSTER PARITY with 33 skills once lane A lands), doctor rc=5 0 fail, grep -i 'free' clean except the MIT-history sentence.
