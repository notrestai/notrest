# Commission — WORKSHOP REBUILD (docs lane D, persistent). model: opus — tier: judgment (curriculum over 32 skills; every claim must reproduce live).
Banked before dispatch by the Director (session 3). Owner's outline, verbatim (2026-09-01): "first thing is the plugin structure and architecture. then the tools categories and what each category is, then each tool with explanation, then a schematic simulation of a session and how its working and how we can add an ew sesion, or other tools, etc." Repeated 2026-09-02 as the go.
Inputs: briefs/commission-2026-09-01-workshop-outline.md · briefs/workshop-plan-2026-09-01.md (the read-only plan: 7-category taxonomy, module plan, session schematic, add-a-session/add-a-tool recipes, 7 open decisions) · docs/MAP.md · docs/CAPABILITIES.md · the tree at 21aa5f8 = v4.6.2.

## Director decisions (the plan's §4, decided so the build is not blocked; disclosed to the owner)
1. SHAPE: the pack becomes the owner's four movements as four module files under docs/workshop/modules/: 01-structure-and-architecture.md · 02-tool-categories.md · 03-the-tools.md · 04-a-session-schematically.md. The ten modules 00-09 are RETIRED (deleted); 00's skill-contract + exit-code opener is folded into 01, 09's close into FACILITATOR.md. README.md, BRIEF.md, DESCRIPTION.md, FACILITATOR.md, handouts/cheatsheet.md, handouts/exercises.md are REWRITTEN to the new arc (exercises re-keyed to the four movements). Net golden-surface change: -10 +4 files.
2. DEPTH: "each tool with explanation" = one section per tool, uniform template — what it is · when it runs or is invoked · inputs/outputs · its script + fixture (if any) · exit codes · ONE honest example: the exact command and its live output on this repo. Cover all 32 skills, all 11 lifecycle hooks + the 2 plumbing files + hooks.json, and the named instruments inside their owning skill. Tools that resist the taxonomy (introspect, compile, recap, game-forge, gate-check.py) get one sentence saying why.
3. TAXONOMY: adopt the plan's 7 categories (A Bookends & posture · B Instruments · C Knowledge verbs · D Judgment verbs · E Arrangements · F Bridges · G Specialists) as the pack's canon; movement 02 defines each category by its JOB IN A SESSION before naming members; add one cross-reference line in docs/MAP.md §3 pointing at the workshop taxonomy so the pack is not a fourth disagreeing list.
4. SESSION SIMULATION: movement 04 = one real prompt traced end to end through all 7 events with the hook, the file it writes, and the exit code at each step (an ASCII sequence diagram plus prose), then "add a new session" (role BY PROMPT — there is no NOTREST_ROLE in the plugin; establish in a fresh project with the real exit codes 6 -> 0; continuation at tier 0) and "add a new tool" (the 8-step recipe from the plan: skill dir + front-matter, worker contract, script, fixture, cited paths, router + oracle parity, golden surface, doctor/eval expectations).
5. AUDIENCE/LENGTH: a reference pack first; FACILITATOR.md carries a 3-hour room path that drives ~10 tools and reads the rest, and a full-day path. Version-specific numbers: exit codes yes; counts only where the pack also states the command that prints the live number; doctor is taught as "exit 5, 0 fail", never the warn tally; fixtures are "30 fixture files + 1 arm"; hooks are "11 lifecycle hooks" per MAP's canon paragraph.
6. VERSION: the seat ships the pack as v4.6.3 (docs release) after the gates; the lane never touches CHANGELOG, manifests, or the HTML.

TOUCH-ONLY: docs/workshop/** (create, rewrite, delete) · evals/golden-release-surface.txt (ONLY the docs/workshop/ lines: remove the 10 retired module paths, add the 4 new ones) · docs/MAP.md (one cross-reference line in §3 only).
LAWS: every command the pack tells a reader to run is RUN BY YOU on this repo and its exit code/output pasted as the example; no invented numbers; no NOTREST_ROLE; no consumer plugin install flow; no commits/staging; no sub-agents; keep eval RELEASE-SURFACE green at the end (`eval.py check --root .` exit 0 requires the golden list to match the files that exist and every touched docs/ path to be listed).
DONE-WHEN: `ls docs/workshop/modules` = exactly the 4 files; eval exit 0; doctor exit 5 with 0 fail; every `python3 …`/`bash …` line in the pack re-run in a final pass with its exit code matching what the text says; a per-tool coverage table in your return (32 skills + 13 hook files, each with its section anchor); RETURN under 60 lines.
