# Commission — 4.6.2 build lane S: SCRIPTS + GATES (doctor.py is a ledger consumer — the refuter round covers you)
COMMISSION (Director session 3, banked 2026-09-01T23:45Z before dispatch; owner GO 2026-09-01: 'fix the remaining items as well and then ship the 4.6.2'). Repo: /Users/ethanabot/Desktop/oracle-suite-plugin, HEAD 54cb763 = v4.6.1. The findings you are fixing are in docs/DOCKET-4.6.2.md (read it first; every item carries a repro).
LAWS: (1) TOUCH-ONLY below is absolute — any file outside it is the seat's or another lane's; if a fix needs a file outside scope, STOP and return the exact need. (2) Red-first: every behavior fix ships with a fixture arm that was watched RED on the old code and GREEN on the new (paste both). (3) Never run the CLI's consumer plugin flow (install/update/marketplace subcommands) on this machine. (4) Never write COORD.md, COORD-AGENTS.md, spend/ledger.md, briefs/, CHANGELOG.md, any manifest version, docs/oracle-skill-flow.html, evals/golden-release-surface.txt — the seat stamps the release. (5) Never spawn sub-agents. (6) Never git commit/push/stage; leave changes in the working tree. (7) Python 3.9 stdlib only; macOS stock tools only (no timeout binary, no gdate, no flock binary).
RETURN (<= 60 lines): per item — what changed (file:line), the red-then-green fixture evidence, exit codes of the fixture(s) you touched run whole; then 'git diff --stat' of your scope; then anything left undone and why. No prose beyond that. You are a PERSISTENT lane: the seat will resume you for feedback rounds, so keep your own working notes.
TOUCH-ONLY: plugins/notrest/skills/recap/scripts/** ; plugins/notrest/skills/introspect/scripts/** and plugins/notrest/skills/introspect/SKILL.md ; plugins/notrest/skills/compile/scripts/compile.py and plugins/notrest/skills/compile/SKILL.md ; plugins/notrest/skills/doctor/scripts/doctor.py, plugins/notrest/skills/doctor/scripts/fixture.sh, plugins/notrest/skills/doctor/SKILL.md ; .gitignore ; the directory plugins/notrest/graph/ (git rm it — this is the ONE staging action you are allowed, because deletion is the fix).
ITEMS: E1 — recap walk.py: the STAMP-ORDER inventory row (:675) must carry the keys emit_inventory (:712) and cmd_prefill (:974) read; walk/walk --json/walk --since/prefill all exit 0 on THIS estate (currently KeyError rc=1); fixture arm: a scratch COORD with one out-of-order stamp, RED on old code. F4 — score_snapshot.py: missing --output-file → one-line refusal, exit 2, like plan_lint/runbook_lint/verdict_lint; make the documented verbless form (introspect/SKILL.md:85) visible in --help (epilog or a real 'score' alias — keep both forms working); arm for both. F5 — compile.py scan: print a progress line to stderr at least every ~10s or per estate volume (so a 117s scan is visibly alive), and state the measured runtime class in compile/SKILL.md next to the scan command; do NOT optimize without profiling — if a profile shows one cheap hot spot (>50%% of wall time) you may fix it and must report before/after seconds. F6 — remove plugins/notrest/graph/ from the tree (git rm -r) and add an anchored ignore rule for it; extend doctor check_gitignore to FAIL when derived graph/compile output is TRACKED under plugins/*/ (i.e. plugins/<p>/graph/ or plugins/<p>/compile/ holding tracked *.json/*.html); arm RED on a scratch tree with a tracked plugins/x/graph/graph.json. M3 — doctor gates: (i) ROSTER PARITY: every skill dir must be NAMED in README.md's skill table (a row whose bold name is the dir), in plugins/notrest/README.md, in the marketplace notrest entry description, and in docs/TUTORIAL.md — FAIL listing the absent names per source (this is RED on the real tree today: beam/mentor/tieredswarm; that is the expected red until the docs lane lands — prove it red on the real tree and green on a fixture); (ii) RENDER SURFACES: also parse the 'N-skill' phrase in docs/oracle-skill-flow.html and FAIL when N != dir count (RED today: 31 vs 32); document the new/changed checks in doctor/SKILL.md's check list.
DONE-WHEN: recap fixture, introspect fixture, compile fixture, doctor fixture all green from a scratch copy with the new arms; 'python3 -m py_compile' on every .py you touched; doctor.py check on the real tree reports the expected reds (ROSTER, RENDER count) and nothing else new; return shape satisfied.
