# Commission — 4.6.1 audit lane C: DOCS & CLAIMS vs THE TREE
COMMISSION (Director session 3, banked 2026-09-01T21:26Z before dispatch). Repo: /Users/ethanabot/Desktop/oracle-suite-plugin, HEAD 54cb763 = v4.6.1. Package under audit: plugins/notrest (32 skills, 13 hooks, 58 scripts).
TOUCH-ONLY: NOTHING inside the repo may be modified — this is a find-only audit (worker never fixes). Scratch work only under /private/tmp/claude-501/-Users-ethanabot-Desktop-oracle-suite-plugin/530a1524-2861-4fec-a687-2c01420a1a15/scratchpad/<lane>/ (make a copy of the tree there if a fixture needs a writable root).
HARD BANS: never run the CLI's consumer plugin flow on this machine — no plugin install, no plugin update, no marketplace add/update subcommands (the installed copy shadows the live skills-dir runtime — proven twice; the pretool gate will refuse and you must not override it). Never write to spend/ledger.md, COORD.md, COORD-AGENTS.md, briefs/. Never spawn sub-agents.
RETURN SHAPE (tight, <= 80 lines): (1) FINDINGS table — severity (ERROR = broken behavior / FIX = wrong but working / MISSING = promised but absent) · file:line · one-sentence defect · REPRO: one exact command and its observed output/exit code · label [verified]/[unverified]. (2) CLEAN list — every check you ran that found nothing, named positively (an absence is only evidence when the check is recorded). (3) Anything you could not check and why. No prose beyond that.
SCOPE: what the package PROMISES vs what it SHIPS. Check: (a) CHANGELOG.md sections 4.6.1, 4.6.0, 4.5.1, 4.5.0 — for each bullet that names a mechanism (a flag, a hook branch, a file, an arm, a count, a timing) locate it in the tree and label it [verified]/[missing]/[contradicted] with file:line; (b) the WORKSHOP PACK named in 4.6.1 (find it; 16 files claimed) — every version-specific statement in it (version numbers, skill counts, exit codes, verb names, command lines) checked against the 4.6.1 tree; (c) START-HERE.md, HANDOFF.md, STATE.md, README.md (repo root) and plugins/notrest/README.md — every command line they tell a reader to run: does it run as written (read-only ones: run them; mutating ones: check the flag exists via --help); every version/skill-count/commit they state vs reality; (d) every SKILL.md: flags, verbs, file paths and other skills it cites — do they exist; the oracle SKILL.md routing table vs hooks/router.sh verbs; (e) CLAUDE.md protocol block in this repo vs what establish.py currently writes into a fresh project (run establish.py --help and, on a scratch git repo under /private/tmp/claude-501/-Users-ethanabot-Desktop-oracle-suite-plugin/530a1524-2861-4fec-a687-2c01420a1a15/scratchpad/docs-lane/, a real establish with the surface flag it documents; diff the block it writes against this repo's block); (f) MISSING: things a consumer would expect from the manifests/README that have no file — e.g. a LICENSE file matching 'MIT', a marketplace-level README, install instructions that match the current CLI, a CHANGELOG entry for every version stamped anywhere.
DONE-WHEN: return shape satisfied; sections (a)-(f) each appear in FINDINGS or CLEAN; every CHANGELOG 4.6.x bullet accounted for by label.
