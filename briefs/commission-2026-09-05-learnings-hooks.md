# Commission — 4.6.3 LEARNINGS LOOP, lane H: HOOKS (KERNEL — refuter round follows)
COMMISSION (Director session 3, banked 2026-09-05T04:52Z before dispatch; owner GO 2026-09-05: 'its a go for me build test and ship as 4.6.3'). Repo: /Users/ethanabot/Desktop/oracle-suite-plugin, HEAD 21aa5f8 = v4.6.2; the tree carries only the refuter-clean eval.py release-surface fix (uncommitted, ships in 4.6.3). FEATURE: THE LEARNINGS LOOP — lessons are banked as validated records, delivered to every session and lane, and audited. Design of record: COORD.md 2026-09-05 04:45Z + 04:47Z lines and this brief.
RECORD (lane S owns the schema, everyone consumes it): archive/findings.jsonl gains kind="learning" with fields: id (L-<n>, assigned by index.py), ts (UTC), tag in {INHERITED,RULED,LEARNED}, statement (<=300 chars), evidence (list, >=1: a COORD timestamp '[YYYY-MM-DD HH:MMZ]', a briefs/ path, a commit hash, or a finding id), scope (list, >=1: a path glob, a skill name, or 'estate'), source ('seat' or a lane id). index.py add refuses a learning missing evidence or scope. index.py gains 'learnings' with --digest (framed '| ' lines, <=200 bytes each, newest first, max N) and --scope <tokens...> (records whose scope matches any token or is 'estate'). DIGEST FORMAT (shared by packet, spawn-gate, router): '| L-<n> [TAG] <statement clipped> — evidence: <first>'.
LAWS: TOUCH-ONLY is absolute; red-first fixture arm for every behavior; no commits/staging; never CHANGELOG/manifests/HTML/golden list (seat); no consumer plugin flow; no sub-agents; py3.9 stdlib, stock macOS; every hook keeps its fast path and fail-open law; you are PERSISTENT — feedback resumes you. RETURN <= 60 lines: per item what changed (file:line), red->green counts, fixture totals, live eval/doctor exit codes, anything left undone.
TOUCH-ONLY: plugins/notrest/hooks/** ; plugins/notrest/skills/eval/scripts/hooks-stdin-fixture.sh, pretool-fixture.sh, router-fixture.sh (arms only).
DEPENDS ON lane S's index.py verbs ('learnings --digest --scope ... --limit', 'learnings --since', 'learnings --trigger-regex'); until they land, build against the interface above and finish when they do (the seat will ping you).
ITEMS: (1) completion-gate.sh (Stop): new rule after the gates check — find the newest learning record ts (index.py learnings --json --limit 1); scan the active COORD.md volume for trigger lines newer than it (regex from index.py --trigger-regex; if no learning exists, scan the last 50 lines); if any exist and no learning cites their timestamps → block with decision:block + reason naming the first trigger line's timestamp and the exact command: python3 $CLAUDE_PLUGIN_ROOT/skills/archivist/scripts/index.py add --kind learning --tag LEARNED --statement '...' --evidence '[ts]' --scope '...'. Loop guard: stop_hook_active as today; fail open + silent on any error; existing NOTREST_GATE_OVERRIDE stays loud. (2) spawn-gate.sh (PreToolUse Agent): after the deny checks pass, compute scope tokens from the tool_input.prompt (path-like substrings and known skill names), ask index.py for a digest (--limit 5, scope tokens + estate), and when non-empty return hookSpecificOutput.updatedInput with prompt = original + '\n\n[notrest LEARNINGS — banked lessons in scope; read before acting]\n' + digest (docs: updatedInput is a partial object; last PreToolUse writer wins — spawn-gate is the ONLY hook that may touch the Agent prompt). Never alter a denied call. Keep deny behavior byte-identical (all 6 pretool-fixture spawn arms). (3) router.sh (UserPromptSubmit): stays fork-free on the miss path — only when the prompt contains a path-like token or a skill name AND archive/findings.jsonl exists, ask index.py for a 1-record scoped digest and print at most one line '[notrest] a banked lesson applies: L-n … (index.py learnings --scope …)'. Measure the miss-path cost before/after (must stay within a few ms). (4) hooks.json: no new entries; timeouts unchanged unless you defend a change.
ARMS (red-first): Stop blocks on an uncited trigger line and passes once a citing learning exists; passes when no trigger exists; fails open silently on a corrupt store; spawn-gate injects the digest (prompt contains the framed block, deny cases unchanged, no injection when the store is empty), router prints one line on a scoped match and nothing otherwise; every hook still returns inside its stdin bound on an idle stdin.
DONE-WHEN: hooks-stdin 32+new arms green, pretool 117+new green, router 31+new green from a cp -p scratch copy; live eval rc=0, doctor rc=5 0 fail; e2e CLI-shape run of the three hooks with real payloads pasted.
