# lane brief — agent-a7c667d554bad0657

- extracted: 2026-09-02 01:13Z
- agent: a7c667d554bad0657
- model: claude-opus-5
- transcript: /Users/ethanabot/.claude/projects/-Users-ethanabot-Desktop-oracle-suite-plugin/530a1524-2861-4fec-a687-2c01420a1a15/subagents/agent-a7c667d554bad0657.jsonl

Auto-extracted by the notrest SubagentStop hook: the first user-role
message of the agent transcript — the exact prompt the seat passed to
this lane. Reproduced verbatim below; never edited, never summarized.

---

model: opus — tier: judgment (curriculum design over a 32-skill harness). READ-ONLY planning lane for the notrest workshop rebuild. Repo: /Users/ethanabot/Desktop/oracle-suite-plugin (working tree is mid-release at v4.6.2; treat the tree as the truth, HEAD is one version behind). Read FIRST: briefs/commission-2026-09-01-workshop-outline.md (the owner's four movements, verbatim). Then read the existing pack docs/workshop/** (16 files: BRIEF, DESCRIPTION, FACILITATOR, README, handouts/cheatsheet + exercises, modules 00-09), docs/MAP.md (the complete plugin map), docs/CAPABILITIES.md, docs/UNDERSTANDING.md, plugins/notrest/README.md, the 32 skills/*/SKILL.md descriptions (front-matter only is enough for most), plugins/notrest/hooks/hooks.json, and evals/golden-release-surface.txt. Modify NOTHING. Deliver, under 120 lines: (1) a proposed taxonomy of the tools — categories with a one-line definition each, and every one of the 32 skills + the 13 hooks + the instruments (doctor/eval/spend/graph/cockpit/establish) placed in exactly one category, with the evidence for the placement (which doc says so); flag any tool that resists categorization. (2) A module-by-module plan for the rebuilt pack in the owner's order (structure/architecture → categories → each tool → schematic session simulation → adding a session / adding a tool), naming for each module: its learning goal, the files it draws on, which existing module content is REUSED vs RETIRED, and its version-specific claims that must reproduce live (exit codes, counts, command lines) — because the pack is on the golden release surface and RELEASE-SURFACE names every file. (3) The session simulation: a step-by-step schematic of one real session in this estate (SessionStart hook + brief packet → UserPromptSubmit nudges/router → work → Agent spawn through spawn-gate → SubagentStop receipt → Stop completion gate → SessionEnd cushion → the successor's packet), citing the hook and script for each step, plus the exact steps to add a NEW session (role by prompt/NOTREST_ROLE, establish in a fresh project, continuation) and to add a NEW tool (skill dir, SKILL.md front-matter, script + fixture, router verb, golden surface, doctor/eval expectations). (4) Open decisions the owner must make (audience, length, whether the pack keeps the 10-module shape or moves to the four movements as files, which of the 32 tools get a full page vs a table row). Do not write any workshop file; return the plan only.
