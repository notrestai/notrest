# 4.7.0 lane D2 — docs + skill text
COMMISSION (Director session 3, banked 2026-09-05T05:16Z before dispatch; owner GO 2026-09-05 for 4.7.0 — the docket is docs/DOCKET-4.7.md, read it first). Repo /Users/ethanabot/Desktop/oracle-suite-plugin, HEAD 21aa5f8 = v4.6.2; the tree carries the in-flight 4.6.3 learnings loop (folds into 4.7.0). LAWS: TOUCH-ONLY absolute; red-first arms; no commits/staging; never CHANGELOG/manifests/HTML/golden list (seat); no consumer plugin flow; no sub-agents; py3.9 stdlib, stock macOS; hooks keep fast paths and fail-open; persistent lane — feedback resumes you; the seat relays cross-lane interfaces, so put your CLI/JSON contracts in your FIRST return line. RETURN <= 60 lines: per item what changed (file:line), red->green counts, fixture totals, live eval/doctor exit codes, anything undone.
TOUCH-ONLY: skills/{archivist,fable-mode,agentswarm,notrest,doctor,eval,sessionend,compile,watch}/SKILL.md (compile: prose, not the doctrine section lane C edits) ; README.md ; plugins/notrest/README.md ; docs/MAP.md content ; docs/CAPABILITIES.md ; docs/TUTORIAL.md ; docs/DOCKET-4.7.md (status column only) ; docs/workshop/** only for changed counts.
ITEMS: every docket item A-I documented where the estate, hooks or the verb are described; the agentswarm brief TEMPLATE gains the mandatory four-box card at the end of every return (D) and the DONE-WHEN/CHECK block convention (I); compile SKILL.md prose: the owner ruling that adoption is automatic under green gates, one candidate per session stays, the daemon never spends tokens unattended; sweep golden docs for counts (eval 15 -> as landed, doctor 12 -> as landed) printing the command beside each number. Verify every claim against the tree at the END of your run; describe only what landed.
DONE-WHEN: eval rc=0, doctor rc=5 0 fail; every cited command run as written.
