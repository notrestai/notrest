# Commission — 4.6.2 build lane H: HOOKS (KERNEL SURFACE — a refuter round follows you)
COMMISSION (Director session 3, banked 2026-09-01T23:45Z before dispatch; owner GO 2026-09-01: 'fix the remaining items as well and then ship the 4.6.2'). Repo: /Users/ethanabot/Desktop/oracle-suite-plugin, HEAD 54cb763 = v4.6.1. The findings you are fixing are in docs/DOCKET-4.6.2.md (read it first; every item carries a repro).
LAWS: (1) TOUCH-ONLY below is absolute — any file outside it is the seat's or another lane's; if a fix needs a file outside scope, STOP and return the exact need. (2) Red-first: every behavior fix ships with a fixture arm that was watched RED on the old code and GREEN on the new (paste both). (3) Never run the CLI's consumer plugin flow (install/update/marketplace subcommands) on this machine. (4) Never write COORD.md, COORD-AGENTS.md, spend/ledger.md, briefs/, CHANGELOG.md, any manifest version, docs/oracle-skill-flow.html, evals/golden-release-surface.txt — the seat stamps the release. (5) Never spawn sub-agents. (6) Never git commit/push/stage; leave changes in the working tree. (7) Python 3.9 stdlib only; macOS stock tools only (no timeout binary, no gdate, no flock binary).
RETURN (<= 60 lines): per item — what changed (file:line), the red-then-green fixture evidence, exit codes of the fixture(s) you touched run whole; then 'git diff --stat' of your scope; then anything left undone and why. No prose beyond that. You are a PERSISTENT lane: the seat will resume you for feedback rounds, so keep your own working notes.
TOUCH-ONLY: plugins/notrest/hooks/** ; plus ONE new fixture file plugins/notrest/skills/eval/scripts/hooks-stdin-fixture.sh (create it; the existing router-fixture.sh / pretool-fixture.sh may gain arms but nothing else under skills/ is yours).
ITEMS: E2 — every hook that reads stdin (router.sh, spawn-gate.sh, agent-ledger.sh, completion-gate.sh, session-end.sh, and pretool-gate.sh which already does it right) must read with a bounded wait; use the pretool-gate.sh:34 pattern (builtin 'IFS= read -r -d \"\" -t N RAW') or an equivalent that cannot hang on an open idle stdin and still reads a full multi-KB JSON payload that arrives at once; python hooks that json.load(sys.stdin) get the bytes handed to them by the shell wrapper, not a live stdin. Then hooks.json: declare an explicit 'timeout' on EVERY event entry (choose values you can defend: the Stop entry already has 60). The fixture: for each of the six hooks, a fifo held open by a sleeper — assert rc=0 and completion within the bound (watched RED on the 4.6.1 hooks: rc=124 under an 8s cap), AND assert a normal payload still produces the hook's real effect (spawn-gate still denies model-omitted / fork / haiku; router still emits its nudge for a research-shaped prompt; agent-ledger still appends a receipt to a scratch COORD-AGENTS.md). F7 — session-start.sh banner: when CLAUDE_PLUGIN_ROOT is unset, derive the plugin root from $0's directory so the version prints (arm: env -u CLAUDE_PLUGIN_ROOT → banner shows v4.6.1, not v?); completion-gate.sh: EMPTY stdin → silent exit 0 (nothing to check), MALFORMED non-empty payload → keep the honest fail-open notice; normalize the mode of all 12 hook files to 0755 (hooks.json stays 0644). Keep every hook's fast path fast — the pretool-gate budget comment is law.
DONE-WHEN: new fixture green from a scratch copy; router-fixture.sh, pretool-fixture.sh still green; 'bash -n' on all 12; every hook exits 0 silently on a minimal payload of its event with cwd outside any estate; return shape satisfied.
