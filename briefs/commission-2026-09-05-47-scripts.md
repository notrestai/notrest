# 4.7.0 lane S — archivist, establish, eval, doctor
COMMISSION (Director session 3, banked 2026-09-05T05:16Z before dispatch; owner GO 2026-09-05 for 4.7.0 — the docket is docs/DOCKET-4.7.md, read it first). Repo /Users/ethanabot/Desktop/oracle-suite-plugin, HEAD 21aa5f8 = v4.6.2; the tree carries the in-flight 4.6.3 learnings loop (folds into 4.7.0). LAWS: TOUCH-ONLY absolute; red-first arms; no commits/staging; never CHANGELOG/manifests/HTML/golden list (seat); no consumer plugin flow; no sub-agents; py3.9 stdlib, stock macOS; hooks keep fast paths and fail-open; persistent lane — feedback resumes you; the seat relays cross-lane interfaces, so put your CLI/JSON contracts in your FIRST return line. RETURN <= 60 lines: per item what changed (file:line), red->green counts, fixture totals, live eval/doctor exit codes, anything undone.
TOUCH-ONLY: skills/archivist/scripts/**, skills/notrest/scripts/establish.py + fixture.sh, skills/eval/scripts/eval.py + fixture.sh, skills/doctor/scripts/doctor.py + fixture.sh.
ITEMS: docket A (finish: arming floor, JSON contract, add flags, LEARNING-LOOP, ESTATE detail) · C (kinds open + alternative with required fields; result requires ran/command/exit; 'index.py card [--json] [--scope]' renders TESTS/OPEN/FINDINGS/LEARNINGS with counts and checkbox lines; 'learnings --triggers' also flags not-tested/could-not-verify/unverified headlines lacking an open record — the regex stays in one place) · E ('index.py promote L-n' copies a learning with scope library to the shelf; 'learnings --digest --library' reads it; establish.py packet carries LIBRARY LEARNINGS newest 2 when the shelf exists) · H (doctor: one WARN 'LOOP HEALTH' with uncited triggers, open by age, learnings/week, drafted-undecided candidates read from compile/candidates.json) · packet carries the card counts line.
DONE-WHEN: archivist/notrest/eval/doctor fixtures green from a scratch copy incl. every new arm; live eval rc=0 (LEARNING-LOOP PASS on this estate — the 4 learnings + any open you must add to make it honest), doctor rc=5 0 fail.
