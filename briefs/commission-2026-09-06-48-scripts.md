# 4.8.0 lane S — establish gate + instruments (establish.py KERNEL — refuter round follows)
COMMISSION (Director session 3, banked 2026-09-06T08:13Z before dispatch; owner GO 2026-09-06 'repo is private now, go for 4.8'). Repo /Users/ethanabot/Desktop/oracle-suite-plugin, HEAD afe2270 = v4.7.1, origin PRIVATE. Docket: docs/DOCKET-4.8.md (read first). LAWS: TOUCH-ONLY absolute; red-first arms; no commits/staging; never CHANGELOG/manifest versions/HTML/golden list (seat); no consumer plugin flow; no sub-agents; never claude -p or a real auto-run; py3.9 stdlib, stock macOS; hooks keep fast paths + fail-open; persistent lane — feedback resumes you; put every CLI/file contract in your FIRST return line. RETURN <= 60 lines.
TOUCH-ONLY: skills/notrest/scripts/establish.py + fixture.sh, skills/eval/scripts/eval.py + fixture.sh, skills/doctor/scripts/doctor.py + fixture.sh.
ITEMS: establish.py: `establish` and `check` refuse without a valid access key with NEW exit 7 and one line naming ~/.notrest/access-key and the owner; `continuation --brief` emits NO packet (exit 7, no END marker, so session-start never injects); `establish --atlas` additionally installs the bank git hook via `atlas.py wire` (call the script; do not copy logic). Share the key check with lane H by CALLING the same verifier (`atlas.py key --check`) — never a second implementation. doctor: an ACCESS KEY line (present/valid/absent/revoked/keyring-empty) as a detail, plus WARN when the keyring is empty; an ATLAS line (bank hook wired? last snapshot age; hub configured?) via atlas.py status. eval: new check ACCESS-GATE — every hook that writes or injects is silent without a key and the deny rules still deny (drive the hooks with the key removed), plus the keyring file exists with a header. Golden surface additions are the seat's.
DONE-WHEN: notrest/eval/doctor fixtures green from a scratch copy; live eval rc=0 (17 checks), doctor rc=5 0 fail with the seat's key present.
