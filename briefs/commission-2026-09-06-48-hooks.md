# 4.8.0 lane H — hooks: the access-key gate (KERNEL — refuter round follows)
COMMISSION (Director session 3, banked 2026-09-06T08:13Z before dispatch; owner GO 2026-09-06 'repo is private now, go for 4.8'). Repo /Users/ethanabot/Desktop/oracle-suite-plugin, HEAD afe2270 = v4.7.1, origin PRIVATE. Docket: docs/DOCKET-4.8.md (read first). LAWS: TOUCH-ONLY absolute; red-first arms; no commits/staging; never CHANGELOG/manifest versions/HTML/golden list (seat); no consumer plugin flow; no sub-agents; never claude -p or a real auto-run; py3.9 stdlib, stock macOS; hooks keep fast paths + fail-open; persistent lane — feedback resumes you; put every CLI/file contract in your FIRST return line. RETURN <= 60 lines.
TOUCH-ONLY: plugins/notrest/hooks/** (except atlas-bank-hook.sh, lane A's), arms in skills/eval/scripts/{hooks-stdin,pretool,router}-fixture.sh.
ITEMS: docket A hook half: a shared fast check in estate-root.sh (`nr_access_ok`: NOTREST_ACCESS_KEY env or ~/.notrest/access-key file, sha256 against plugins/notrest/.access/keys.sha256; python only when the file exists; cache the verdict per session under pulse/ so the per-prompt hooks pay one builtin read); session-start.sh prints exactly one line when the key is absent/invalid ("[notrest] notrest is part of Atlas — no valid access key on this machine; ask the owner. The harness is inactive here.") and nothing else (no packet, no laws); coord-nudge, router, spawn-gate digest injection, agent-ledger banking, completion-gate loop, pre-compact, session-end cushion all exit 0 silently without a key — EXCEPT the deny rules of spawn-gate and pretool-gate, which stay active (a machine without a key must not become a machine without laws). NOTREST_UNATTENDED runs require the key too. Arms: no key -> banner line only + every writer silent + deny rules still deny; valid key -> unchanged behavior (all existing arms); revoked hash -> treated as no key; malformed keyring -> fail CLOSED for the banner (inactive) but hooks still silent. Empty keyring (no hashes) = the seat has not minted yet -> treated as inactive, stated in the line.
DONE-WHEN: hooks-stdin 104+, router 40+, pretool 124+ green from a cp -p copy; live eval rc=0, doctor rc=5 0 fail with the seat's key present.
