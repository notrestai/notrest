# lane brief — agent-a47c85e26054d479b

- extracted: 2026-07-27 19:40Z
- agent: a47c85e26054d479b
- model: claude-opus-5
- transcript: /Users/ethanabot/.claude/projects/-Users-ethanabot-Desktop-oracle-suite-plugin/97c59e4b-e7d2-4827-b4bf-2b918d5221d2/subagents/agent-a47c85e26054d479b.jsonl

Auto-extracted by the notrest SubagentStop hook: the first user-role
message of the agent transcript — the exact prompt the seat passed to
this lane. Reproduced verbatim below; never edited, never summarized.

---

BUILDER lane, domain: ARCHIVIST validator (R12 enforcement). Repo: /Users/ethanabot/Desktop/oracle-suite-plugin (HEAD c35ce95, v3.15.0, 30 skills). Working tree only; NO commits, NO bumps. TOUCH ONLY: plugins/notrest/skills/archivist/scripts/index.py, plugins/notrest/skills/archivist/scripts/fixture.sh, plugins/notrest/skills/archivist/SKILL.md.

CONTEXT — read record F-19 in archive/findings.jsonl first (statement carries the law): once two estates each number their own records, a BARE `F-<n>` is ambiguous — the same token names different records in different stores, so a re-recorded sentence like "F-14 amends F-9" resolves to the wrong decisions in the wrong house. The law is already adopted: every CROSS-ESTATE reference is qualified `project:F-<n>`; only internal links use bare local ids. The law currently has no enforcement — that is what you build. Precedent: this estate's doctrine is that an unenforced law is a habit, and habits are weaker than checks.

BUILD — a WARN-grade door check in `add` (never a rejection; this is prose, and prose has legitimate reasons to name a record):
1. Scan the record's `statement` and `ask` for bare `F-<n>` tokens (word-bounded; a token already qualified as `<project>:F-<n>` is NOT bare and must never be flagged — that is the whole point).
2. A bare `F-<n>` is CLEAN when the id appears in this record's own `links` array, or in an evidence ref of type `record` (local form). Otherwise it is SUSPECT.
3. On any SUSPECT token: the record STILL LANDS (exit 0, id printed to stdout as always — do not break `out="$(… add …)"` captures), and a warning goes to STDERR naming each token and the rule, e.g. `warn: unqualified-record-ref — statement names F-9 which is not in links; cross-estate references must be qualified <project>:F-9`.
4. Add `--strict-refs` which promotes the warning to a rejection (exit 2, rule name `unqualified-record-ref`) for callers that want it enforced hard. Default stays WARN.
5. `track`/`library find` unaffected. No change to any existing validator rule, exit code, or output shape on the happy path — this estate's own fixtures depend on them.
6. SKILL.md: document the rule under the citation grammar section — the law, the WARN default, `--strict-refs`, and the honest line that a warn is a nudge and only `--strict-refs` is a gate.

FIXTURE (extend the existing 144-assertion suite, keep every one green): bare F-n in statement not in links → lands + stderr warns + stdout still just the id; same token present in links → no warn; qualified `rig:F-9` → never warned (must not false-positive); qualified token adjacent to a bare one → only the bare one warns; evidence-record-ref local form counts as declared; `--strict-refs` → exit 2, rule named, nothing appended (verify the store line count is unchanged); multiple suspects → all named in one warn block; a statement mentioning "F-" with no digits or "F-9x" → not flagged (no false positives); ask-field detection; and a no-regression pass proving the 144 existing assertions still hold.

SELF-CHECK: run the archivist fixture; run eval.py check --root . and doctor.py check --root . (both must stay exit 0 — you are adding a warn path, not changing shipped counts); py_compile index.py. Report exact counts and exits.

RETURN (tight): line delta; the warn message format verbatim; fixture counts before/after; instrument exits; any false-positive class you found and how you excluded it; deliberately-not-done.
