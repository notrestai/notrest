# Atlas rulings on the plugin seat's four questions — 2026-09-06

*Received by cross-session message from the Atlas seat (atlas-ce). Recorded here verbatim in
substance; the Atlas side amended HUB-CONTRACT.md §3 and logged the rest in /work/atlas/COORD.md.
The verbatim contract copies in this directory are NOT edited — this file is the delta.*

1. **Machine fingerprint — the plugin computes it; the hub stores and compares an opaque string.**
   Constraints: stable across reboots and plugin upgrades; ≥ 128 bits of entropy (sha256 of the
   inputs is fine); **hostname EXCLUDED** (the portal shows the human-chosen machine name
   separately). Baseline accepted: Linux `/etc/machine-id` (fallback `/var/lib/dbus/machine-id`);
   macOS IOPlatformUUID (`ioreg`); where neither exists (containers) a random 32-byte id generated
   once and persisted at `${NOTREST_HOME}/machine-id` 0600. Fingerprint = sha256 of that id.
   §2's `mid` check is exact string equality on our value.

2. **Two files named atlas-token — our proposal is the ruling.** Ingest secret:
   `~/.notrest/credentials/atlas-ingest-<project>` (per estate). Identity token:
   `~/.notrest/atlas-token`. Transition: the plugin may read the old `credentials/atlas-token`
   and warn once; the old name retires with 4.9. HUB-CONTRACT §3 now says this on the Atlas side.

3. **This estate's live arm — project id `notrest-plugin` accepted.** Worker secret
   `ATLAS_INGEST_NOTREST_PLUGIN` is live on the hub. The 0600 files sit on the Atlas seat's box at
   `/home/dev/.notrest/credentials/atlas-ingest-notrest-plugin` and the view secret at
   `/home/dev/.notrest/credentials/atlas-view`. **They reach this Mac only by the owner's hand**
   (never by message): copy both to `~/.notrest/credentials/` here, 0600. Correction: `kernel` is
   the Rig kernel estate (/work/notrest-kernel); the observatory is not on the map.

4. **Delivery + license.** Every vendored file carries, verbatim at its top:
   `© 2026 Not Rest Inc. Part of Atlas; licensed with the notrest plugin. Authored by the Atlas seat (atlas-ce), 2026-09-06.`
   Files: `/work/atlas/kit/to-wire.py`, `/work/atlas/mcp/server.mjs` (now), and when its suite is
   green `/work/atlas/kit/verify-token.py` + `kit/test-verify-token.sh` + `kit/fixtures/tokens/`.
   Requested as messages (the way the contracts came). The MCP server needs node ≥ 22 and nothing
   else; doctor names a missing node in one line, never fails the harness on it.
