# Commission — 4.6.1 audit lane B: SCRIPTS & FIXTURES
COMMISSION (Director session 3, banked 2026-09-01T21:26Z before dispatch). Repo: /Users/ethanabot/Desktop/oracle-suite-plugin, HEAD 54cb763 = v4.6.1. Package under audit: plugins/notrest (32 skills, 13 hooks, 58 scripts).
TOUCH-ONLY: NOTHING inside the repo may be modified — this is a find-only audit (worker never fixes). Scratch work only under /private/tmp/claude-501/-Users-ethanabot-Desktop-oracle-suite-plugin/530a1524-2861-4fec-a687-2c01420a1a15/scratchpad/<lane>/ (make a copy of the tree there if a fixture needs a writable root).
HARD BANS: never run the CLI's consumer plugin flow on this machine — no plugin install, no plugin update, no marketplace add/update subcommands (the installed copy shadows the live skills-dir runtime — proven twice; the pretool gate will refuse and you must not override it). Never write to spend/ledger.md, COORD.md, COORD-AGENTS.md, briefs/. Never spawn sub-agents.
RETURN SHAPE (tight, <= 80 lines): (1) FINDINGS table — severity (ERROR = broken behavior / FIX = wrong but working / MISSING = promised but absent) · file:line · one-sentence defect · REPRO: one exact command and its observed output/exit code · label [verified]/[unverified]. (2) CLEAN list — every check you ran that found nothing, named positively (an absence is only evidence when the check is recorded). (3) Anything you could not check and why. No prose beyond that.
SCOPE: everything under plugins/notrest/skills/*/scripts (58 files) plus plugins/notrest/hooks/gate-check.py. Check: (a) every .py compiles (python3 -m py_compile) and answers --help (or its documented verb list) with exit 0; every .sh passes 'bash -n'; the .mjs parses under node --check; (b) every SKILL.md invocation of its own script (grep for 'scripts/' lines in each SKILL.md) uses flags/verbs the script actually accepts — run each documented invocation in read-only/check mode against a scratch copy of the repo and record exit code; (c) run every fixture.sh / *-fixture.sh / *-arm.sh from a scratch COPY of the repo (cp -R the repo into /private/tmp/claude-501/-Users-ethanabot-Desktop-oracle-suite-plugin/530a1524-2861-4fec-a687-2c01420a1a15/scratchpad/scripts-lane/repo and run there, never in place) — record pass/fail counts and exit codes; a fixture that cannot run outside the real tree is itself a finding; (d) the ship gate: doctor.py check and eval.py check on the real tree (read-only) — record exit codes and every WARN/FAIL verbatim; find what 'battery 30/0' in the ledger refers to and run it; (e) cross-file cites: paths named inside scripts (other scripts, hooks, docs) that do not exist; (f) python version / dependency assumptions beyond the stdlib; hard-coded absolute paths to this machine (/Users/ethanabot) in shipped scripts.
DONE-WHEN: return shape satisfied; sections (a)-(f) each appear in FINDINGS or CLEAN; every fixture named with its exit code.
